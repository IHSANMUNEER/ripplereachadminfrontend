// SkeletonLoader.js

import React from 'react';
import Skeleton from '@mui/material/Skeleton';

function Skeleton1({ variant, width, height }) {
  return <Skeleton variant={variant} width={width} height={height} />;
}

export  {Skeleton1};
